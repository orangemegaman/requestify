import { ModalPage, ModalPageProps } from '@vkontakte/vkui';

export const ImportModal = (props: ModalPageProps) => {
    return (
        <ModalPage {...props}>
            <div>Загрузить</div>
        </ModalPage>
    );
};
